<tr>
  <td><%= product.name %></td>
  <td><%= product.price %></td>
  <td><%= link_to "Edit", edit_product_path(product) %></td>
  <td><%= link_to "Show", product_path(product) %></td>
  <td>
    <%= link_to "Destroy", product_path(product), method: :delete, data: {confirm:
      "are you sure?"} %>
  </td>
</tr>
